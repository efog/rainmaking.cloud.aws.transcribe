export type Transcript = {
    callId?: string,
    callerId?: string,
    eventTimestamp?: string,
    endTime?: number,
    resultId?: string,
    speakerName?: string,
    startTime?: number,
    transcript?: string
}
